# OpenClaw 완벽 매뉴얼
> AI 기반 여행/문화 블로그 자동화 시스템 구축 가이드

**버전:** 1.0.0
**최종 업데이트:** 2026-02-05
**목표:** 월 1,000명 방문자 달성 (4개월 내)

---

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [사전 준비사항](#2-사전-준비사항)
3. [환경 설정](#3-환경-설정)
4. [프로젝트 구조](#4-프로젝트-구조)
5. [콘텐츠 생성 워크플로우](#5-콘텐츠-생성-워크플로우)
6. [이미지 시스템](#6-이미지-시스템)
7. [블로그 배포](#7-블로그-배포)
8. [문제 해결 가이드](#8-문제-해결-가이드)
9. [개발 히스토리](#9-개발-히스토리)

---

## 1. 프로젝트 개요

### 1.1 OpenClaw란?

OpenClaw는 AI를 활용하여 SEO 최적화된 여행/문화 블로그 콘텐츠를 자동으로 생성하는 시스템입니다.

```
[주제 입력] → [AI 텍스트 생성] → [AI 이미지 생성] → [Hugo 블로그] → [GitHub Pages 배포]
```

### 1.2 핵심 기능

| 기능 | 설명 | 기술 |
|------|------|------|
| 텍스트 생성 | SEO 최적화된 블로그 포스트 | Ollama (로컬 LLM) |
| 커버 이미지 | 실사 사진 자동 검색 | Unsplash API |
| 인라인 이미지 | 인포그래픽, 지도, 비교표 | Gemini AI |
| 블로그 빌드 | 정적 사이트 생성 | Hugo + PaperMod |
| 배포 | 무료 호스팅 | GitHub Pages |

### 1.3 왜 이 조합인가?

- **Ollama**: 로컬 실행으로 API 비용 없음, 빠른 생성
- **Gemini**: 무료 이미지 생성 (일일 50장), 한국어 텍스트 지원
- **Hugo**: 빠른 빌드, SEO 친화적, 무료
- **GitHub Pages**: 무료 호스팅, 자동 HTTPS

---

## 2. 사전 준비사항

### 2.1 필수 소프트웨어

```bash
# 1. Node.js (v18 이상)
# https://nodejs.org/ 에서 다운로드

# 설치 확인
node --version  # v18.x.x 이상
npm --version   # 9.x.x 이상

# 2. Git
# https://git-scm.com/ 에서 다운로드

git --version   # 2.x.x 이상

# 3. Hugo Extended
# https://gohugo.io/installation/ 참고

hugo version    # hugo v0.120.0+extended 이상 (extended 필수!)

# 4. Ollama
# https://ollama.ai/ 에서 다운로드 및 설치
```

### 2.2 Ollama 모델 설치

```bash
# Ollama 실행 후
ollama pull qwen3:8b

# 설치 확인
ollama list
# NAME       SIZE
# qwen3:8b   4.9 GB
```

> **모델 선택 가이드:**
> - `qwen3:8b`: 한국어 최적화, 권장
> - `llama3.1:8b`: 영어 콘텐츠용
> - `gemma2:9b`: 균형잡힌 성능

### 2.3 API 키 발급

#### Unsplash API (커버 이미지)

1. https://unsplash.com/developers 접속
2. 회원가입 후 "New Application" 클릭
3. 앱 이름, 설명 입력 후 생성
4. **Access Key** 복사

#### Gemini API (AI 이미지)

1. https://aistudio.google.com/ 접속
2. Google 계정으로 로그인
3. 좌측 메뉴 "Get API key" 클릭
4. "Create API key" → 키 복사

> **무료 한도:**
> - Unsplash: 50 requests/hour
> - Gemini: 무료 티어에서 이미지 생성 가능

---

## 3. 환경 설정

### 3.1 프로젝트 복제

```bash
# 1. 저장소 복제
git clone https://github.com/your-username/openclaw.git
cd openclaw

# 2. 의존성 설치
npm install
```

### 3.2 환경 변수 설정

```bash
# .env.example을 복사하여 .env 생성
cp .env.example .env
```

`.env` 파일 편집:

```env
# ========================================
# Ollama 설정 (필수)
# ========================================
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# ========================================
# Unsplash API (커버 이미지)
# ========================================
UNSPLASH_ACCESS_KEY=your_unsplash_access_key_here

# ========================================
# Gemini API (AI 이미지 생성)
# ========================================
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_IMAGE_ENABLED=true
GEMINI_IMAGE_MAX_COUNT=50
GEMINI_IMAGE_MODEL=gemini-2.0-flash-exp

# ========================================
# Hugo 설정
# ========================================
HUGO_BASE_URL=/travel-blog
```

### 3.3 시스템 상태 확인

```bash
npm run status
```

예상 출력:
```
🔍 OpenClaw 시스템 상태 확인

Ollama 상태:
  ✓ 서버 연결 정상 (http://localhost:11434)
  ✓ 모델: qwen3:8b

이미지 서비스:
  ✓ Unsplash: API 키 설정됨
  ✓ Gemini: API 키 설정됨, 이미지 생성 활성화

디렉토리:
  ✓ drafts/ 존재
  ✓ blog/content/posts/ 존재
  ✓ blog/static/images/ 존재
```

---

## 4. 프로젝트 구조

### 4.1 디렉토리 구조

```
openclaw/
├── .env                    # 환경 변수 (git에 포함 안됨)
├── .env.example            # 환경 변수 템플릿
├── package.json            # npm 설정
├── tsconfig.json           # TypeScript 설정
│
├── src/                    # 소스 코드
│   ├── cli/                # CLI 명령어
│   │   ├── index.ts        # CLI 진입점
│   │   └── commands/       # 개별 명령어
│   │       ├── new.ts      # 새 포스트 생성
│   │       ├── drafts.ts   # 초안 목록
│   │       ├── review.ts   # 초안 검토
│   │       ├── publish.ts  # 발행
│   │       └── status.ts   # 상태 확인
│   │
│   ├── generator/          # 콘텐츠 생성
│   │   ├── index.ts        # 생성 오케스트레이션
│   │   ├── ollama.ts       # Ollama API 클라이언트
│   │   ├── prompts.ts      # LLM 프롬프트 템플릿
│   │   ├── frontmatter.ts  # Hugo 프론트매터 생성
│   │   ├── content-parser.ts   # 이미지 마커 파싱
│   │   └── image-prompts.ts    # 이미지 프롬프트
│   │
│   ├── images/             # 이미지 처리
│   │   ├── unsplash.ts     # Unsplash API (커버)
│   │   └── gemini-imagen.ts # Gemini AI (인라인)
│   │
│   └── seo/                # SEO 최적화
│       └── optimizer.ts
│
├── scripts/                # 유틸리티 스크립트
│   ├── regenerate-gyeongju-images.mts
│   └── generate-all-post-images.mts
│
├── drafts/                 # 생성된 초안
│   └── *.md
│
├── data/                   # 런타임 데이터
│   └── gemini-usage.json   # API 사용량 추적
│
├── blog/                   # Hugo 블로그 (별도 git 저장소)
│   ├── hugo.toml           # Hugo 설정
│   ├── content/posts/      # 발행된 포스트
│   │   ├── travel/
│   │   └── culture/
│   ├── static/images/      # 이미지 저장소
│   └── public/             # 빌드 결과물
│
└── MANUAL.md               # 이 문서
```

### 4.2 데이터 흐름

```
                          ┌─────────────────────┐
                          │     주제 입력       │
                          │  "경주 여행 가이드"  │
                          └──────────┬──────────┘
                                     │
                                     ▼
              ┌──────────────────────────────────────────┐
              │            Ollama (qwen3:8b)             │
              │  SEO 최적화 블로그 포스트 텍스트 생성    │
              │  + 이미지 마커 [IMAGE:style:description] │
              └──────────────────────┬───────────────────┘
                                     │
                    ┌────────────────┴────────────────┐
                    │                                 │
                    ▼                                 ▼
        ┌───────────────────┐            ┌───────────────────┐
        │   Unsplash API    │            │    Gemini API     │
        │   커버 이미지     │            │   인라인 이미지   │
        │   (실사 사진)     │            │   (AI 생성)       │
        └─────────┬─────────┘            └─────────┬─────────┘
                  │                                 │
                  └────────────────┬────────────────┘
                                   │
                                   ▼
                    ┌──────────────────────────┐
                    │      drafts/*.md         │
                    │   초안 저장 (검토 대기)   │
                    └──────────────┬───────────┘
                                   │
                                   ▼ (npm run publish)
                    ┌──────────────────────────┐
                    │  blog/content/posts/     │
                    │     발행 포스트          │
                    └──────────────┬───────────┘
                                   │
                                   ▼ (hugo --minify)
                    ┌──────────────────────────┐
                    │     blog/public/         │
                    │   정적 HTML 빌드         │
                    └──────────────┬───────────┘
                                   │
                                   ▼ (git push)
                    ┌──────────────────────────┐
                    │    GitHub Pages          │
                    │  https://xxx.github.io/  │
                    └──────────────────────────┘
```

---

## 5. 콘텐츠 생성 워크플로우

### 5.1 새 포스트 생성

#### 기본 생성 (텍스트만)

```bash
npm run new -- -t "경주 여행 가이드" --type travel
```

#### 이미지 포함 생성

```bash
npm run new -- -t "경주 여행 가이드" --type travel --inline-images --image-count 3
```

#### 비대화 모드 (자동 승인)

```bash
npm run new -- -t "경주 여행 가이드" --type travel --inline-images --yes
```

### 5.2 명령어 옵션

| 옵션 | 설명 | 예시 |
|------|------|------|
| `-t, --topic` | 포스트 주제 (필수) | `"부산 해운대 맛집"` |
| `--type` | 콘텐츠 유형 | `travel` 또는 `culture` |
| `--inline-images` | AI 인라인 이미지 생성 | - |
| `--image-count` | 생성할 이미지 수 | `3` (기본값) |
| `--yes, -y` | 모든 확인 자동 승인 | - |
| `--length` | 포스트 길이 | `short`, `medium`, `long` |

### 5.3 콘텐츠 유형별 특징

#### travel (여행)
- 실용 정보 중심 (교통, 비용, 시간)
- 추천 코스 및 동선
- 맛집/카페 정보
- 여행 팁

#### culture (문화)
- 전시/공연 정보
- 작가/작품 소개
- 관람 포인트
- 관람객 추천

### 5.4 초안 관리

```bash
# 초안 목록 확인
npm run drafts

# 출력 예시:
# 📄 초안 목록 (drafts/)
#
# 1. 2026-02-04-1.md
#    제목: 경주 여행 완벽 가이드
#    상태: 검토 대기
#    이미지: 3장

# 초안 검토/편집
npm run review -- -f 2026-02-04-1.md

# 초안 발행
npm run publish -- -f 2026-02-04-1.md
```

---

## 6. 이미지 시스템

### 6.1 하이브리드 이미지 전략

OpenClaw는 두 가지 이미지 소스를 사용합니다:

| 용도 | 소스 | 특징 |
|------|------|------|
| 커버 이미지 | Unsplash | 고품질 실사 사진 |
| 인라인 이미지 | Gemini AI | 맞춤형 인포그래픽/지도 |

### 6.2 이미지 스타일

```typescript
type ImageStyle =
  | 'infographic'  // 여행 다이어리 페이지 (교통, 비용, 시간)
  | 'diagram'      // 보물지도 여정 (교통/동선)
  | 'map'          // 친구가 그려준 약도 (코스 안내)
  | 'comparison'   // 카페 칠판 메뉴판 (맛집 비교)
  | 'moodboard'    // 감성 콜라주 (분위기 전달)
  | 'bucketlist';  // 게이미피케이션 체크리스트
```

### 6.3 이미지 마커 시스템

포스트 생성 시 LLM이 이미지 삽입 위치에 마커를 생성합니다:

```markdown
## 교통 정보

서울에서 경주까지 KTX로 2시간...

[IMAGE:infographic:경주 교통 및 비용 요약]

## 추천 코스

황리단길에서 시작하는 코스...

[IMAGE:map:경주 1박2일 추천 코스 지도]
```

시스템이 마커를 실제 이미지로 교체:

```markdown
![경주 교통 및 비용 요약](/travel-blog/images/inline-xxx-1.jpeg)
*서울에서 경주까지 KTX 2시간, 1박2일 약 26만원 예상*
```

### 6.4 이미지 프롬프트 전략

**핵심 원칙: 정보 전달 → 감성적 매력 + 공유 유도**

| 기존 (기능적) | 개선 (감성적) |
|--------------|--------------|
| "가격 비교 표" | "카페 칠판 메뉴판 느낌" |
| "교통 다이어그램" | "보물지도 여정" |
| "코스 지도" | "친구가 그려준 약도" |

**좋은 프롬프트 예시:**

```
경주 1박2일 여행 필수 정보 (Korean Travel Journal Page)

[교통편]
• 서울→경주 KTX: 2시간, 편도 45,000원
• 경주역→시내: 버스 50번/700번 20분

[예산 가이드]
교통비: 90,000원 (KTX 왕복)
숙박: 80,000원 (한옥스테이)
...

Hand-drawn travel journal aesthetic, watercolor stamps, ticket stubs collage feel.
```

### 6.5 기존 포스트에 이미지 추가

```bash
# scripts/add-images.mts 수정 후 실행
npx tsx scripts/add-images.mts
```

### 6.6 API 사용량 관리

```bash
# 사용량 확인
cat data/gemini-usage.json
# {"date":"2026-02-05","count":16}
```

| 설정 | 값 |
|------|-----|
| 일일 한도 | 50장 |
| 경고 임계값 | 80% (40장) |
| 포스트당 최대 | 5장 |

---

## 7. 블로그 배포

### 7.1 Hugo 블로그 설정

#### hugo.toml 주요 설정

```toml
baseURL = 'https://your-username.github.io/travel-blog/'
languageCode = 'ko'
title = '여행 블로그'
theme = 'PaperMod'

[params]
  env = "production"
  defaultTheme = "auto"
  ShowReadingTime = true
  ShowShareButtons = true
  ShowPostNavLinks = true
  ShowBreadCrumbs = true
  ShowCodeCopyButtons = true
```

### 7.2 로컬 미리보기

```bash
# Hugo 개발 서버 실행
cd blog
hugo serve -D -p 1313

# 브라우저에서 접속
# http://localhost:1313/travel-blog/
```

> `-D` 옵션: 초안(draft: true)도 표시

### 7.3 GitHub Pages 배포

#### 방법 1: 수동 배포

```bash
# 1. Hugo 빌드
cd blog
hugo --minify

# 2. 변경사항 커밋
git add .
git commit -m "feat: 새 포스트 추가"

# 3. GitHub에 푸시
git push origin main
```

#### 방법 2: GitHub Actions 자동 배포

`.github/workflows/hugo.yml` 생성:

```yaml
name: Deploy Hugo site to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.155.2
    steps:
      - name: Install Hugo CLI
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb \
          && sudo dpkg -i ${{ runner.temp }}/hugo.deb

      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build with Hugo
        run: hugo --minify

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./public

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
```

### 7.4 GitHub Pages 설정

1. GitHub 저장소 → Settings → Pages
2. Source: "GitHub Actions" 선택
3. 또는 Branch: `main`, Folder: `/public` 선택

### 7.5 커스텀 도메인 (선택사항)

1. 도메인 구매 (예: Namecheap, GoDaddy)
2. DNS 설정:
   ```
   Type: CNAME
   Name: www
   Value: your-username.github.io
   ```
3. GitHub Pages → Custom domain에 도메인 입력

---

## 8. 문제 해결 가이드

### 8.1 Ollama 연결 실패

**증상:**
```
Error: connect ECONNREFUSED 127.0.0.1:11434
```

**해결:**
```bash
# 1. Ollama 실행 확인
ollama serve

# 2. 다른 터미널에서 테스트
curl http://localhost:11434/api/tags
```

### 8.2 이미지 404 오류

**증상:** Hugo에서 이미지가 표시되지 않음

**원인:** Hugo baseURL과 이미지 경로 불일치

**해결:**
```markdown
# 잘못된 예
image: "/images/cover.jpg"

# 올바른 예
image: "/travel-blog/images/cover.jpg"
```

**코드 수정:** `src/images/gemini-imagen.ts`에서:
```typescript
const baseURL = process.env.HUGO_BASE_URL || '/travel-blog';
const relativePath = `${baseURL}/images/${filename}`;
```

### 8.3 Gemini API 오류

**증상:**
```
Gemini API 오류: 400 - Invalid API key
```

**해결:**
1. `.env` 파일의 `GEMINI_API_KEY` 확인
2. Google AI Studio에서 키 재발급
3. `GEMINI_IMAGE_ENABLED=true` 확인

### 8.4 TypeScript 타입 오류

**증상:**
```
error TS18046: 'data' is of type 'unknown'
```

**해결:** API 응답에 인터페이스 정의
```typescript
interface GeminiResponse {
  candidates?: Array<{
    content?: {
      parts?: Array<{
        inlineData?: { data: string; mimeType: string };
      }>;
    };
  }>;
}

const data = await response.json() as GeminiResponse;
```

### 8.5 Git 중첩 저장소 경고

**증상:**
```
warning: adding embedded git repository: openclaw/blog
```

**원인:** `blog/` 폴더가 별도 git 저장소

**해결:**
```bash
# blog/는 별도로 관리
cd openclaw
git rm --cached blog  # 메인 저장소에서 제외

cd blog
git add .
git commit -m "..."
git push origin main
```

### 8.6 환경변수 로드 실패

**증상:** 스크립트에서 API 키를 찾지 못함

**해결:** 스크립트 상단에 dotenv 추가
```typescript
import { config } from 'dotenv';
config();  // 반드시 최상단에!
```

---

## 9. 개발 히스토리

### 9.1 프로젝트 타임라인

#### Phase 1: 기본 구조 구축
- TypeScript + Commander.js CLI 설정
- Ollama 연동 및 텍스트 생성
- Hugo 블로그 기본 설정

#### Phase 2: 이미지 시스템 구현
- Unsplash API 연동 (커버 이미지)
- Gemini AI 연동 (인라인 이미지)
- 하이브리드 이미지 전략 수립

#### Phase 3: 이미지 품질 개선
- 프롬프트 전략 전환 (정보 → 감성)
- 6가지 이미지 스타일 정의
- 포스트별 맞춤 이미지 생성

#### Phase 4: 배포 및 문서화
- GitHub Pages 배포
- 문제 해결 및 경로 수정
- 매뉴얼 작성

### 9.2 주요 결정사항

| 결정 | 이유 |
|------|------|
| Ollama 사용 | 로컬 실행으로 비용 없음, 빠른 반복 |
| qwen3:8b 모델 | 한국어 품질 우수 |
| Gemini 이미지 | 무료 한도 충분, 한국어 텍스트 지원 |
| Hugo + PaperMod | 빠른 빌드, SEO 최적화, 한국어 지원 |
| 하이브리드 이미지 | 실사(신뢰감) + AI(맞춤 정보) 조합 |

### 9.3 발생했던 문제들

1. **이미지 404**: Hugo baseURL 불일치 → 환경변수로 해결
2. **타입 오류**: API 응답 타입 미정의 → 인터페이스 추가
3. **스크립트 환경변수**: dotenv 누락 → 명시적 로드
4. **Git 중첩**: 별도 저장소 관리로 해결
5. **이미지 매력도**: 프롬프트 전략 전환

### 9.4 현재 상태

**발행된 포스트:** 6개
- 경주 여행 완벽 가이드
- 부산 해운대 맛집 베스트 4
- 2월 겨울 국내여행 베스트 5
- 서울 북촌한옥마을 완벽 가이드
- 국립현대미술관 서울 2026 기획전
- (기존 포스트)

**생성된 AI 이미지:** 15장+
**API 사용량:** 16/50 (일일)

---

## 부록

### A. 유용한 명령어 모음

```bash
# 시스템 상태 확인
npm run status

# 새 포스트 생성 (이미지 포함)
npm run new -- -t "주제" --type travel --inline-images

# 초안 목록
npm run drafts

# 초안 발행
npm run publish -- -f filename.md

# Hugo 로컬 서버
cd blog && hugo serve -D

# Hugo 빌드
cd blog && hugo --minify

# 블로그 배포
cd blog && git add . && git commit -m "update" && git push
```

### B. 환경 변수 전체 목록

```env
# Ollama
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# Unsplash
UNSPLASH_ACCESS_KEY=xxx

# Gemini
GEMINI_API_KEY=xxx
GEMINI_IMAGE_ENABLED=true
GEMINI_IMAGE_MAX_COUNT=50
GEMINI_IMAGE_MODEL=gemini-2.0-flash-exp

# Hugo
HUGO_BASE_URL=/travel-blog
```

### C. 참고 자료

- [Hugo 공식 문서](https://gohugo.io/documentation/)
- [PaperMod 테마](https://github.com/adityatelange/hugo-PaperMod)
- [Ollama 공식 사이트](https://ollama.ai/)
- [Google AI Studio](https://aistudio.google.com/)
- [Unsplash Developers](https://unsplash.com/developers)
- [GitHub Pages 문서](https://docs.github.com/pages)

---

**문서 작성:** Claude (Anthropic)
**최종 검토:** 2026-02-05
